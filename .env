# --- General Configuration ---
# Your server's public URL/Domain for the API.
SERVER_URL=https://whatsapp.change-this-domain.com

# 🔑 MASTER PASSWORD for the API
# This is the "API Global Token" you will use in Odoo.
# CHANGE THIS NOW to something strong (e.g., Use `openssl rand -hex 32`)
AUTHENTICATION_API_KEY=CHANGE_THIS_TO_STRONG_PASSWORD_123

# --- Database (PostgreSQL) ---
# Do not change unless you have a specific need.
DB_host=evolution_postgres
DB_user=evolution
DB_password=evolution_pass
DB_database=evolution_db
DB_port=5432

# --- Cache (Redis) ---
# Required for session storage and performance.
CACHE_REDIS_ENABLED=true
CACHE_REDIS_URI=redis://evolution_redis:6379/0
CACHE_REDIS_PREFIX=evolution:

# --- Evolution Settings ---
# Instance Deletion Policy: false = Keep session on logout; true = Delete session.
DEL_INSTANCE_ON_LOGOUT=false
qrcode_limit=30
